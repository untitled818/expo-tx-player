{
  "name": "expo-tx-player-example",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "start": "npx expo start",
    "android": "npx expo run:android --variant debug",
    "ios": "npx expo run:ios",
    "prebuild": "npx expo prebuild",
    "web": "npx expo start --web",
    "build:test": "NODE_ENV=production eas build --local --profile release-local --platform android"
  },
  "dependencies": {
    "expo": "~53.0.9",
    "expo-dev-client": "^5.1.8",
    "react": "19.0.0",
    "react-native": "0.79.2"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@types/react": "~19.0.10",
    "typescript": "~5.8.3"
  },
  "private": true,
  "expo": {
    "autolinking": {
      "nativeModulesDir": ".."
    }
  }
}
